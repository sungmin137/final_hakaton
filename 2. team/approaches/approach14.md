# 접근 14 — 접근 1 점수(cw_) 고도화 (Count-Weight Plus)

## 개요
9차 설정 모델의 분기 이득 77%가 접근 1의 클래스별 집약 점수 140개에서 나온다(3. docs/07 §6). 원본 열을 늘리는 피처는 트리가 안 쓰므로,
**새 정보를 같은 "클래스별 집약 점수" 형태로** 넣는다. 나머지(v4 피처, colsample 0.7, 3차 복원 배율, 쌍둥이 규칙)는 13차 구성 그대로 고정.
코드: `4. src/common/approach14_cw_plus/cw_plus.py`, 피처 종류 `--features v4p` (= v4 + cwp_*).

| 확장 | 토큰 | 뜻 | 열 수 |
|---|---|---|---|
| (i) 유형 분리 | `유전자:missense/lof/syn` (gene 단위), `유전자:변이` (missense만, variant 단위) | 같은 유전자라도 '어떻게' 바뀌었는지 구분한 NB 점수 | 28 + 28 |
| (ii) 위치 구간 | `유전자:b{위치//25}` (기능성 변이) | hotspot one-hot이 희소해 못 쓰인 정보를 25aa 구간으로 묶어 점수화 | 28 |
| (iii) 군집 상대 | 저변이 군집(SARC·PRAD·PCPG·THYM·OV), 상피암 군집(STES·LUSC·HNSC·LUAD·BLCA) 안 log-softmax | 군집 안에서만의 상대 순위 | 6 + 6 |

- 점수는 Bernoulli NB 로그우도비(토큰 수로 평균), float64 + 소수점 4자리(재현성), 학습 fold에서 2명 미만 토큰은 0.
- 학습 행은 내부 5-Fold OOF, **쌍둥이는 하나로만 센다**(접근 7의 붕괴 교훈). test는 fit 없이 transform.

## 버전
| 버전 | 파일 | 구성 | 정직 CV (배율 적용) | 저변이 군집 F1 | 상피암 군집 F1 | LB |
|---|---|---|---|---|---|---|
| v1 | (미제출) | v4p(3확장 96열 전부) + colsample 0.7 + 복원 배율 | 0.4582 / 배율 0.4780 | 0.281 | 0.425 | - |
| v2 | (미제출) | 유형 분리(유전자 단위)만 + 군집 상대, 토큰 없는 행은 0 | 0.4608 / 배율 0.4767 | 0.273 | 0.423 | - |
| **v3** | `approach14_v3_20260913_2132` | **9차 모델과 v2 모델의 로그 확률 평균(w=0.5)** + 복원 배율 + 규칙 | 배율 **0.4941** (+0.0094) | 0.303 | 0.457 (31~100구간) | **0.45641 (16차 규칙 미적용본 = 17차 규칙본 0.4564122, 팀 최고)** |
| v4 | `approach14_v4_20260913_2150` | v3(w=0.5) + 혜림 HNSC/STES 소프트 라우터(approach19, conf≥0.7) + 규칙 | (v3와 동일 모델) | | | 미제출 |
| v5 | `approach14_v5_20260913_2150` | v3에서 w=0.3만 변경(9차 0.7 : v2 0.3) + 규칙 | 0.4931 | | 0.441 | **0.4563172 (18차, 16차 −0.0001)** |
| v6 | `approach14_v6_20260913_2150` | v3에서 w=0.7만 변경(9차 0.3 : v2 0.7) + 규칙 | | | | 미제출 |
| v7 | (CV만, 미제출) | v4p2 = v4p + **위치 구간(25aa) 점수를 변이 31개 이상 행 한정**(그 행들로만 NB fit) | 단독 0.4614 / 배율 0.4763, 9차와 w0.5 블렌드 **0.4919 (16차 0.4941보다 낮음)** | | 31~100: 단독 0.428 (v2 0.457) | - |
| v8 | (CV만, 미제출) | 16차 블렌드 확률에 **쌍 재판기를 확률 단계에서 결합**(쌍 합 유지, 비율만 기하 혼합 v): 혜림 HNSC/STES 상위40 LR, 혜성 GBMLGG/LGG 마커 6개 LR | 최고 0.4943 (16차 0.4941) — 이득 없음 | | | - |
| **v9** | `approach14_v9_20260914_0010` | 16차 블렌드 + **세 번째 모델: 이진 유전자 행렬 다항 LR(C=1.0)** 로그 결합 w3=0.2 + 배율 + 규칙 | **0.5010 (+0.007)**, 절반 교차확인 +0.007/+0.004 | | 31~100: 0.436→0.459 | **0.4428 (19차, 16차 −0.0136, 폐기)** |
| v10 | (CV만, 미제출) | 새 토큰 4종 각각 v4p에 추가: `pos`(정확 위치) / `prop`(치환 성질) / `pair`(상위 40 유전자 공변이 쌍) / `band`(변이 밴드별 유형 통계) | 단독 0.4882 / 0.4828 / 0.4690 / 0.4712, 9차 블렌드 최고 0.4972(pos+band) — 전부 +0.003 이하 | | 31~100 개선 없음 | - |
| **v10** | `approach14_v10_20260914_0110` | **9차 0.4 / v2 0.3 / 접근 7 전처리 확장 모델(a7) 0.3 로그 결합** + 배율 + 규칙 | **0.5049 (+0.0108)**, 절반 교차 +0.009/+0.013 | | 31~100: 0.436→0.449 | **0.4483 (20차, 16차 −0.0081)** |
| **v11** | `approach14_v11_20260914_1627` | **v4s(v4 + 치환 스펙트럼) 모델 0.5 + v2(저장) 0.5** + 배율 + 규칙 | **0.5137 (+0.0196)**, 절반 교차 +0.025/+0.015 | | 31~100 0.464, 101+ 0.396 | **0.4752 (23차, 팀 최고)** |
| v12 | `approach14_v12_20260914_1627` | 9차 .25 / v2 .25 / v4s .25 / v4sp .25 | 0.5172 (+0.0231), 절반 교차 +0.030/+0.017 | | 31~100 0.467 | 후보(v11 뒤) |
| (기준) 9차 | approach12_v4 | v4 + colsample 0.7 + 복원 배율 | 0.4847 | | | 0.4377 |

## v1 결과와 원인
- 전체 −0.0087, 배율 적용 −0.0067. 저변이 군집 0.303→0.281. **THYM −0.187, LAML −0.102, TGCT −0.054**가 손실의 대부분.
- 원인: 변이가 1~3개뿐인 클래스(THYM·LAML·THCA)는 새 토큰(missense 변이 단위, 25aa 구간)이 거의 없어 점수가 사전확률 상수가 되고, 희소 토큰 점수가 노이즈로 작용. 반면 CESC +0.036, PCPG +0.041, STES +0.040, PAAD +0.031은 올라 방향 자체는 유효.
- v2: 희소한 변이 단위·구간 점수를 빼고 유전자 단위 유형 분리 점수만 유지, 토큰 없는 행은 0(사전확률 미부여).

## v2 결과와 v3의 근거
- v2 전체는 여전히 −0.006(배율 0.4767)이고 THYM −0.171, TGCT −0.079가 남았다. 그러나 **변이 수 구간별**로 보면 방향이 명확하다:

| 변이 수 | n | 9차 | v2 |
|---|---|---|---|
| 0~10 | 2,488 | 0.242 | 0.241 |
| 11~30 | 2,333 | 0.387 | 0.375 |
| **31~100** | 993 | 0.403 | **0.457** |
| 101+ | 387 | 0.359 | **0.384** |

- 새 점수는 test에 2.4배 많은 31~100개 구간에서 +0.054, 초과변이 구간에서 +0.025를 준다. 저변이 구간은 9차가 낫다.
- 두 모델의 **로그 확률 평균**이 이를 합친다: w=0.5에서 0.4941(+0.0094). 절반 교차확인 heldout +0.0101 / +0.0038 (fit 최적 w 0.3·0.6). 변이 수 임계 라우팅(T=30)은 +0.003으로 평균보다 못하다.
- 지금까지의 블렌딩(접근 3·5)이 LB에서 실패한 것과의 차이: 그때는 **같은 피처의 다른 모델**을 섞었고, 이번에는 **다른 정보(유형 분리 점수)를 가진 모델**을 섞어 test에 많은 구간을 직접 개선한다. 그래도 LB로만 확정한다.

## v3 LB 결과 (2026-09-13 밤)
- **0.45641** — 규칙 미적용본으로 팀 최고 갱신(13차 0.44235 대비 +0.014, 9차 0.4377 대비 +0.019). LB 편차(±0.015)를 넘는 첫 개선.
- 처음으로 "정직 CV 이득이 LB로 옮겨진" 사례. 이유: 이득이 test에 많은 변이 31~100개 구간에서 나왔고(0.403→0.457), 섞은 두 모델이 **다른 정보**(유전자 유무 vs 변이 유형)를 갖고 있었기 때문.
- 다음: (1) 같은 파일의 쌍둥이 규칙 적용본 제출(+0.005 기대) → 새 기준선. (2) 그 위에 혜림님 HNSC/STES 라우터 한 요소. (3) 블렌드 가중치 w 0.3/0.7 한 번씩. (4) v2 쪽 점수에 위치 구간 점수를 고변이 구간 한정으로 다시 시도.

## v4·v5·v6 — 16차 위에 한 요소씩 (2026-09-13 밤 생성, 미제출)
- 공용 코드 `4. src/common/approach14_cw_plus/blend.py`(`run(name, w, router)`). **두 모델의 test 확률은 16차 저장본을 그대로 재사용**해 재학습 편차(±0.015)를 배제했다.
  9차 확률 = `approach12_v4_20260910_1651/test_proba.npy`(배율 나눔), v2 확률 = `approach14_v3_20260913_2132/test_proba_v2.npy`
  (w=0.5 블렌드 저장본에서 p2 ∝ blend²/p7로 복원; 복원 오차 1e-8, 16차 예측 2,546행 전부 일치 확인).
- **v4 = v3 + 혜림 님 라우터**: approach19 코드 그대로(HNSC/STES 환자만 LR C=0.1, 빈도차 상위 40 유전자, 예측이 두 암종이고 확신 ≥0.7일 때만 교체). 후보 129행 중 **4행 변경**(STES→HNSC 1, HNSC→STES 3). 13차 때도 3행이었으니 라우터 효과는 LB 편차 안일 가능성이 큼 → 제출 우선순위 낮음, 규칙본 다음에.
- **v5 = w 0.3 / v6 = w 0.7**: 16차와 다른 행 각 137 / 136. STES 비율 17.6% / 17.7%(16차 17.8%). 규칙본 파일은 `6. experiments/submissions/twin_rule/`.
- **쌍둥이 규칙 동점 버그 수정(2026-09-13)**: train에 KIPAN·KIRC가 같은 수로 있는 동점 프로파일(test 4행: 366·1071·1761·1833)은 `max(set(labs))`가 프로세스마다 다른 라벨을 뽑아 **규칙본이 실행마다 달라졌다**. 이제 동점이면 모델 답을 유지한다(`postprocess/twin_rule.py`). 16차 규칙본(`approach14_v3_..._twin_rule.csv`)은 그 중 2행(366·1071)이 무작위로 KIPAN(모델 답 KIRC)으로 남아 있다 — 이미 제출본이라 그대로 두고, v4~v6 규칙본은 수정된 규칙으로 생성(→ 16차 규칙본과 비교 시 이 2행만큼 추가 차이). 2행 차이는 LB ±0.001 이내.
- 제출 순서(변경 없음): 16차 규칙본 → v5/v6(w) → v4(라우터). 판단은 LB.

## 17차 결과 — 규칙본 LB 0.4564122368 (2026-09-13 밤)
- 16차 기본본과 **동일 점수**. 규칙이 바꾼 행은 6개(366·1034·1071·1884 KIPAN↔KIRC, 1567 PCPG→GBMLGG, 2163 LGG→GBMLGG)뿐이고 그중 366·1071은 동점 무작위 행이라 실제 정보는 4행. 13차 때(8행, +0.0048)와 달리 v3 블렌드 모델은 쌍둥이 235행의 뒤집힌 라벨을 이미 거의 맞히고 있어 규칙이 얹을 것이 없다.
- 결론: **v3 계열에서 규칙 ON/OFF는 무관**(±0). 원칙은 그대로 규칙 ON 유지(해롭지 않음). 다음 제출은 v5(w 0.3) → v6(w 0.7) → v4(라우터).

## w 스캔 — 어느 후보를 먼저 낼까 (2026-09-13 밤, OOF 재사용, 배율 적용 정직 CV)
| w | 전체 | 0~10 | 11~30 | 31~100 | 101+ |
|---|---|---|---|---|---|
| 0 (9차) | 0.4847 | 0.242 | 0.387 | 0.403 | 0.359 |
| 0.3 (v5) | 0.4931 | 0.251 | 0.387 | **0.441** | 0.372 |
| 0.5 (v3) | **0.4941** | 0.255 | 0.382 | 0.436 | 0.376 |
| 0.7 (v6) | 0.4904 | 0.252 | 0.378 | 0.432 | 0.378 |
| 1.0 (v2) | 0.4767 | 0.241 | 0.375 | 0.457 | 0.384 |
- 0.3~0.6은 평평(±0.002), 0.7부터 전 구간 하락. 절반 교차확인: 짝수 절반 최적 w 0.5, 홀수 절반 최적 0.3. 즉 **w 0.3과 0.5는 구별 불가, 0.7은 열세**.
- 변이 수별 w(≤30 저 / >30 고) 12조합 전부 w=0.5 고정(0.4941)보다 낮음(최고 0.4928). 절반 교차확인에서도 이득 없음 → 변이 수 조건부 가중치는 폐기.
- **한 번만 낼 수 있다면 v5(w=0.3)**: CV 동급, test에 2.4배 많은 31~100 구간 +0.005, 홀수 절반의 최적값. v6은 CV 열세, v4는 4행 변경뿐. 다만 기대 이득은 LB 편차(±0.015) 안이므로 결과가 같아도 놀랍지 않다.

## 18차 결과 — v5(w=0.3) 규칙본 LB 0.4563171813 (2026-09-13 밤)
- 16·17차(0.4564122)와 0.0001 차이. 139행이 바뀌었는데 점수가 같다 → **w 0.3~0.5는 LB에서도 평탄**. CV 스캔과 일치.
- 세 번의 제출(규칙 미적용·규칙·w 0.3)이 전부 0.4563~0.4564: **v3 블렌드 계열의 상한 ≈ 0.456**. 후처리(규칙·라우터)와 가중치로는 더 못 올린다. v6(w 0.7)·v4(라우터)는 제출하지 않는다(CV 열세 / 4행).
- 다음은 **새 정보**를 v2 쪽에 넣는 것뿐: (a) v2 점수에 위치 구간(25aa) 점수를 변이 31개 이상 행에만 부여, (b) 혜성 님 저차원 서브모델(KIPAN/KIRC, GBMLGG/LGG)을 세 번째 모델로 로그평균에 추가, (c) 혜림 님 재판기 쌍 확장을 확률 단계에서 결합. 판단 기준은 그대로 LB, 단 편차 ±0.015를 넘는 것만 채택.

## v7 — 위치 구간 점수 고변이 한정 (2026-09-13 밤, CV만)
- 구현: `cw_plus.py` `tok_bucket_hi`(변이 ≥31 행만 `유전자:b{위치//25}` 토큰, 그 외 빈 토큰 → 점수 0), `TokenNB(fit_tokened_only=True)`(토큰 있는 행만으로 클래스 통계, 없는 클래스는 스무딩), 피처 종류 `--features v4p2`(main.py 선택지 등록). 기존 v4p 열은 바이트 단위로 동일(16차 재현 유지).
- 정직 CV(`6. experiments/2026-09-13_v4p2_xgb_mild_col_grp`), 배율 적용:

| 구성 | 전체 | 0~10 | 11~30 | 31~100 | 101+ |
|---|---|---|---|---|---|
| v2 단독 | 0.4767 | 0.241 | 0.375 | **0.457** | 0.384 |
| v4p2 단독 | 0.4763 | 0.243 | **0.403** | 0.428 | 0.376 |
| 16차 (9차+v2 w0.5) | **0.4941** | 0.255 | 0.382 | 0.436 | 0.376 |
| 9차+v4p2 w0.5 | 0.4919 | 0.251 | 0.399 | 0.432 | 0.371 |
| 9차+v2+v4p2 (0.5/0.25/0.25) | 0.4965 | 0.252 | 0.404 | 0.440 | 0.380 |

- **결론: 이득 없음.** 구간 점수를 넣은 모델은 정작 목표였던 31~100 구간이 v2보다 낮고(0.457→0.428), 대신 점수가 0인 11~30 구간이 올랐다 → 트리 분기가 바뀐 부수 효과(실행 편차) 수준. 클래스별로는 PCPG −0.11, BLCA −0.05 / TGCT +0.06. 세 모델 블렌드 +0.0024는 CV 편차 안이라 제출 가치 없음. (a) 축 폐기.
- 남은 축: (b) 혜성 님 KIPAN/KIRC·GBMLGG/LGG 서브모델을 세 번째 모델로 (c) 혜림 님 재판기 확률 단계 결합. 둘 다 "다른 정보"를 가진 모델이라는 16차의 성공 조건에 부합한다.

## v8 — 쌍 재판기 확률 단계 결합 (2026-09-13 밤, CV만) — (b)·(c) 축 동시 종료
- 코드 `approach14_cw_plus/pair_referee.py`: `PairReferee(pair, mode)`(top40 = 혜림 approach19 재현, markers = 혜성 approach11 v7의 IDH1·EGFR·ATRX·PTEN·IDH2·TP53), `mix_pair(p, A, B, q, v)`: p_A':p_B' = (p_A/p_B)^(1−v)·(q_A/q_B)^v, 쌍 합은 유지. 재판기 OOF는 메인과 같은 정직 분할(쌍둥이 묶음)로 fold 학습 부분만 fit.
- 재판기 단독(쌍 행만, 정직 분할): HNSC/STES 0.643, GBMLGG/LGG 0.649. 혜성 님 문서의 0.73은 비쌍둥이 LOOCV라 기준이 다름.

| 결합 | v | 전체 | HNSC | STES | GBMLGG | LGG |
|---|---|---|---|---|---|---|
| 16차 블렌드(기준) | - | **0.4941** | 0.335 | 0.489 | 0.466 | 0.498 |
| HNSC/STES | 0.35 | 0.4934 | 0.328 | 0.478 | | |
| HNSC/STES | 1.0 | 0.4885 | 0.294 | 0.351 | | |
| GBMLGG/LGG | 0.5 | 0.4943 | | | 0.406 | 0.567 |
| GBMLGG/LGG | 1.0 | 0.4901 | | | 0.322 | 0.562 |
| 둘 다 | 0.35 | 0.4935 | 0.328 | 0.478 | 0.426 | 0.542 |

- **결론: 이득 없음.** HNSC/STES 재판기는 섞을수록 두 클래스가 함께 하락(블렌드가 이미 재판기보다 잘 가름). GBMLGG/LGG는 LGG를 얻는 만큼 GBMLGG를 잃어 합 0. 두 재판기 모두 16차 블렌드에 **없는 정보가 없다**. 라우팅(혜성 v17)·확률 결합(여기) 둘 다 닫힘.
- 오늘 밤 세 축 결과: (a) 위치 구간 고변이 한정 ✗, (b) 혜성 서브모델 확률 결합 ✗, (c) 혜림 재판기 확률 결합 ✗. **16차 = 사실상 최종 후보.** 남은 가능성은 v4/v4p와 귀납 편향이 다른 세 번째 모델(예: 이진 유전자 행렬 선형 모델)을 블렌드 파트너로 쓰는 것뿐이며, 이것도 정직 CV +0.01 이상일 때만 제출.

## v9 — 세 번째 모델: 이진 유전자 선형 모델 (2026-09-14 00:10)
- 아이디어: 16차가 통한 조건(다른 정보를 가진 모델의 로그 결합)을 한 번 더. 트리(9차·v2)와 귀납 편향이 다른 **다항 로지스틱 회귀를 원본 이진 유전자 행렬(4,384열, 변이 유무)** 에 학습. 단독은 약하지만(정직 CV 0.32) 파트너로서 새 정보를 준다. 베르누이 NB는 단독 0.18, 블렌드에서 해로움(폐기).
- 정직 CV(OOF 재사용, 배율 적용), 2모델 블렌드(1−w3)와 LR w3 로그 결합:

| C | w3=0.2 | w3=0.3 | 비고 |
|---|---|---|---|
| 0.2 | 0.4973 | 0.4984 | |
| 0.5 | 0.4996 | 0.4992 | |
| **1.0** | **0.5010** | 0.4979 | 채택(평탄 구간 중앙) |
| 2.0 | 0.5020 | 0.4949 | |

- 기준 0.4941 → 0.5010 (+0.007). 절반 교차확인: 짝수 절반 최적(2.0, 0.2) → 홀수 절반 0.4900 vs 16차 0.4831(+0.007); 홀수 최적(0.5, 0.2) → 짝수 0.5068 vs 0.5029(+0.004). **두 절반 모두 같은 방향.** 구간: 31~100 0.436→0.459(w3 0.3이면 0.476), 101+ 0.376→0.377, 0~10 0.255→0.253. 클래스별: DLBC +0.04, PCPG +0.03, STES +0.02 / LAML·COAD·KIPAN −0.01.
- 제출본: 17차 규칙본과 331행 차이(STES↔COAD 45, UCEC/LUSC/LUAD→STES 41). **STES 예측 비율 17.8%→18.7%** — STES 과잉 예측이 LB 하락과 연관됐던 이력(1차 21.5%)이 있어 주의. 채택 기준 +0.01에는 못 미치지만 패턴이 16차와 같아 제출로 확인(19차). 코드 `blend.py` `third_lr_binary`, `run(..., third=, w3=)`.
- **19차 LB 0.442838734 — 16차 0.45641 대비 −0.0136. 세 번째 모델 축 종료.** 정직 CV +0.007이 LB에서는 −0.014로 뒤집힘. 해석: (1) 바뀐 331행 중 STES로 몰린 41행(UCEC/LUSC/LUAD→STES)이 STES 과잉 예측을 키웠고(17.8→18.7%), 이 클래스는 test에 train보다 많은 31~100 구간에 몰려 있어 CV가 잡지 못한다. (2) 선형 모델은 유전자 유무만 보므로 v2의 "변이 유형" 같은 새 정보가 아니라 9차 정보의 다른 표현에 가까웠다 → 접근 3·5 블렌딩 실패와 같은 유형. **16차의 성공 조건은 "다른 표현"이 아니라 "다른 정보"였다는 것을 재확인.**
- 남은 제출은 쓰지 않는다. 최종 후보 = 16차(규칙 미적용 0.45641) 또는 17차(규칙 적용 0.4564122, 동일). 팀 합의대로 규칙 ON이면 17차.

## v10 — 새 정보 토큰 4종 비교 (2026-09-14 새벽, CV만) — 전부 미달
- 코드 `cw_plus.py`: `tok_pos`, `tok_prop`(아미노산 성질 클래스 H/P/+/−/G 전이), `PairTokenizer`(fit 데이터로 상위 40 유전자 결정), `tok_band_gene`(lo≤10/mid/hi≥31 접두). 피처 종류 `--features v4p:pos|prop|pair|band`(main.py 등록). 정직 CV 폴더 `6. experiments/2026-09-14_v4p:<이름>_xgb_mild_col_grp`.

| 후보 | 단독(배율) | 9차+후보 w0.5 | 최고 조합 | 31~100 | 절반 교차 Δ (짝, 홀) |
|---|---|---|---|---|---|
| 16차 기준 (9차+v2) | 0.4767 | **0.4941** | | 0.436 | 0 / 0 |
| pos 정확 위치 | **0.4882** (단일 모델 최고) | 0.4967 | 0.4972 (9차+pos+band) | 0.427 | +0.0006 / +0.0055 |
| prop 치환 성질 | 0.4828 | 0.4913 | 0.4928 | 0.425 | −0.003 / −0.002 |
| pair 공변이 쌍 | 0.4690 | 0.4929 | 0.4925 | 0.416 | −0.003 / −0.005 |
| band 밴드별 통계 | 0.4712 | 0.4964 | | 0.417 | +0.0009 / +0.0048 |

- **결론: 네 후보 모두 제출 기준 미달.** 최고 이득 +0.003(pos+band)은 CV 편차 안이고, 절반 교차확인이 한쪽만 양수이며, test에 많은 31~100 구간이 어느 조합에서도 기준(0.436)을 넘지 못했다. LB가 올랐던 16차는 이 구간이 +0.03 올랐던 경우라, 지금 후보들은 LB에서 같거나 내려갈 확률이 높다.
- 눈여겨볼 점: `pos` 모델은 단독 0.4882로 9차(0.4847)·v2(0.4767)보다 높은 첫 단일 모델. 그러나 9차와 정보가 많이 겹쳐(둘 다 유전자 유무 + hotspot) 블렌드 이득이 작다. v2+pos 블렌드는 31~100 구간 0.460이지만 전체 0.4822로 9차 없이는 못 쓴다.
- 클래스별(9차+pos): DLBC +0.04, CESC +0.04, UCEC +0.03 / HNSC −0.02, PCPG −0.02, LIHC −0.02 — 상쇄.
- **접근 14의 "클래스별 집약 점수" 레시피는 여기서 소진.** 변이 유형(v2)이 유일하게 새 정보였고, 위치·성질·쌍·밴드는 트리가 이미 쓰는 정보의 재표현이었다.

## 진단 — 점수는 어디서 새는가, 배율 재적합 중첩 CV (2026-09-14 새벽)
- 16차 블렌드 OOF 클래스별 F1(배율 적용): 하위 = SARC 0.20(정밀 0.13) · PAAD 0.30 · CESC 0.30 · PRAD 0.33(정밀 0.23) · HNSC 0.34 · LIHC 0.34 · THYM 0.35 · PCPG 0.35 · LUAD 0.36 · OV 0.37. 상위 = ACC 0.94 · SKCM 0.88 · COAD 0.78 · THCA 0.71.
- SARC는 632행 예측(실제 198), PRAD 664행(실제 266): BRCA 84·KIPAN 63·OV 51행이 SARC로, BRCA 104·THCA 67행이 PRAD로 간다. 3차 배율(SARC 3.0, PRAD 2.0)이 만든 '싱크'.
- **배율 재적합 중첩 CV**(4 fold OOF로 좌표 상승 적합 → 남은 fold 평가, 5회):

| 변형 | 중첩 CV | 31~100 | SARC 예측 수 |
|---|---|---|---|
| 3차 고정(기준) | **0.4941** | 0.436 | 632 |
| 26개 전부 재적합 | 0.4890 | 0.409 | 243 |
| STES·HNSC 고정, 24개 | 0.4864 | 0.401 | 243 |
| SARC·PRAD만 | 0.4936 | 0.436 | 257 |
| 저변이 군집 6개만 | 0.4921 | 0.436 | 246 |

- **결론: 어떤 재적합도 고정 벡터를 못 이긴다.** SARC 과잉 예측은 버그가 아니라 macro F1 최적해다(큰 클래스 BRCA의 정밀도를 내주고 작은 클래스 SARC의 재현율을 사는 교환). 재적합은 fold 안에서만 좋고 밖에서는 나빠져 14·15차 LB 하락과 일치. **배율 축은 이걸로 완전히 닫힘.** "배율은 3차 복원 벡터 고정" 원칙이 중첩 CV로도 확인됐다.
- test 안 동일 행 쌍(train에 없는 것) 22그룹 44행은 17차가 모두 같은 라벨로 예측. 한 쌍을 KIPAN/KIRC로 나눠 배정해도 기대 정답 수는 같아(접근 6 결론과 동일) 이득 0.
- 남는 손실은 변이 0~10개 행(train 40%, F1 0.255)의 저변이 군집이며, 표 안의 정보로는 구분이 안 되는 영역이다.

## 9/14 오전 — 구조 축 세 가지 (A 계층 학습 · B 근사 쌍둥이 · C test 내 동일 행 쌍)
- **A. 계층 학습**: KIPAN/KIRC→KIDNEY, GBMLGG/LGG→BRAIN으로 합쳐 24클래스 XGB(v4, v4p 각각, 정직 CV, `6. experiments/2026-09-14_hier24/`) → 26으로 확장. 24클래스 macro F1은 16차를 접은 것(0.5237)과 계층 모델(0.5239)이 같음 → "쌍둥이 노이즈가 용량을 낭비한다"는 가설 기각. 26클래스 전체: 계층 블렌드(쌍 내부 분할은 16차 비율) 0.4965(+0.0024), 나머지 22클래스 0.5044→0.5074. 편차 안, 미채택.
- **B. 근사 쌍둥이**: test 결측 128행(237셀)을 결측 무시로 train과 재매칭 → 회수 0행. train 안 해밍 거리 1~2 쌍(변이≥5) 3개뿐, test↔train 근사 매칭 0행. 여백 없음.
- **C. test 안 동일 행 쌍 규칙** (`postprocess/test_pair_rule.py`): train에서 변이 ≥3인 완전 동일 행 그룹은 **100% 신장·뇌 쌍**(3~5개 64그룹, 6~9개 103, 10~19개 199, 20+ 56 모두 1.00; 1~2개만 0.86). test 안에서만 동일한 쌍 20개(train에 없음, 변이≥3) 중 17차가 SARC·SARC 2쌍, PRAD·PRAD 2쌍으로 예측 → 확실히 틀림. 블렌드 확률의 신장/뇌 질량으로 쌍을 고르고(4쌍 모두 신장) 한 행씩 KIPAN·KIRC 배정. **8행 변경**, 기대 정답 +4행, SARC·PRAD 오예측 −8. 이미 신장·뇌로 예측된 16쌍은 동일 행이라 확률이 같아 나누는 것이 동전 던지기(기대값 동일)이므로 유지. 파일 `twin_rule/approach14_v3_20260913_2132_twin_rule_testpair.csv`. 기대 LB +0.001~0.003(확실한 방향, 크기 작음). 제출 여부는 사용자 판단.

## 9/14 오전 (2) — 군집 전문가 혼합 · STES 밴드별 배율 (CV만, 둘 다 미달)
- **군집 전문가 확률 혼합**(쌍 재판기의 군집 확장): 군집 행만으로 학습한 LR(이진 유전자)·XGB(v4, depth 4)의 군집 내 분포를, 블렌드의 군집 질량은 유지한 채 기하 혼합. 군집 내 F1은 전문가가 블렌드보다 낮음(저변이 6개: XGB 0.487 vs 블렌드 0.504, 상피암 6개: 0.517 vs 0.537). 전체 혼합 최고 +0.0044(저변이+BRCA·THCA·LAML·TGCT, XGB, v=0.35 → 0.4985). 편차 안.
- **STES(·HNSC·LUSC) 배율을 변이 밴드별로**(중첩 CV, GRID 0.3~2.0): 전체 0.4915 / 0.4923 / 0.4939 — 고정 0.4941보다 낮고 fold마다 배율이 제각각(불안정). **train에서는 31~100 밴드의 STES가 과잉 예측이 아님**(예측 167 / 실제 186). 즉 test의 STES 과잉은 train에서 보이지 않는 분포 이동이라 train 기반 배율로는 교정 불가.

## 9/14 오전 (3) — v2 모델 트리 수·학습률 (CV만, 미달)
| v2 설정 | 단독 | 9차 블렌드 w0.5 | 31~100 | STES 예측(실제 379) | 절반 교차 Δ |
|---|---|---|---|---|---|
| 100그루 lr0.1 (16차) | 0.4767 | **0.4941** | 0.436 | 292 | 0 / 0 |
| 200그루 lr0.1 | 0.4796 | 0.4959 | 0.439 | 317 | +0.001 / +0.003 |
| 300그루 lr0.05 | 0.4844 | 0.4962 | 0.433 | 317 | +0.004 / +0.001 |
- 단독은 오르지만 블렌드 이득 +0.002는 편차 안, STES 예측 증가(+25)는 19차형 LB 하락 위험 신호. 미채택. 폴더 `6. experiments/2026-09-14_v4p_trees/`.

## 9/14 오전 (4) — 점수 열만 쓴 저차원 모델 (CV만, 미달)
- v4p의 클래스별 집약 점수 열만(cw_·cwp_·n_ 계열 191열, 원본 g_ 제외) XGB(depth 4, 150그루) / LR. 단독 0.4748 / 0.4335 — 191열로 v2(4,611열) 0.4767에 근접. 9차+v2+점수XGB w3=0.2 → 0.4972(+0.003), STES 예측 292→306. 편차 안, 미채택. OOF `2026-09-14_v4p_trees/oof_scoresonly_*.npy`.

## v10 — 세 번째 모델: 접근 7 전처리 확장 (2026-09-14 01:10) — 16차 이후 첫 조건 충족
- 접근 7(a7: 기본 + 유형 분리 이진화·도메인 구간·부담 정규화·희귀 변이)은 9/10에 단독 +0.021이었지만 v4에 **합치면** −0.005라 폐기됐던 피처. 16차 레시피(합치지 말고 **다른 정보를 가진 모델로 블렌드**)를 적용. a7 단독 정직 CV 0.4231(약함)이지만 정보가 다르다.

| 구성 | 전체 | 0~10 | 11~30 | 31~100 | 101+ | STES 예측 | 절반 교차 Δ |
|---|---|---|---|---|---|---|---|
| 16차 (9차+v2) | 0.4941 | 0.255 | 0.382 | 0.436 | 0.376 | 292 | 0 / 0 |
| 9차+a7 | 0.4978 | 0.245 | 0.410 | 0.440 | 0.367 | 264 | +0.006 / +0.003 |
| 9차+v2+a7 1/3 | 0.5039 | 0.248 | 0.413 | 0.446 | 0.377 | 283 | +0.010 / +0.011 |
| **9차+v2+a7 .4/.3/.3** | **0.5049** | 0.252 | 0.413 | 0.449 | 0.372 | 281 | **+0.009 / +0.013** |

- **제출 조건 3개 처음으로 모두 충족**: CV +0.01 이상, 31~100 구간 개선, STES 예측 감소(19차 실패 유형의 반대). 클래스별: DLBC +0.10, PAAD +0.05, BLCA +0.05, KIRC +0.03 / OV −0.02, KIPAN·LAML −0.01.
- 제출본: 17차 규칙본과 **308행 차이**. 이동 상위 STES→LUSC 32, STES→COAD 19, COAD→STES 15. 클래스 수 STES −37, BRCA −22 / LUSC +48, OV +11. **STES 비율 17.7% → 16.3%**. 두 번 실행 md5 동일. 코드 `blend.py third_a7`, `run(w=3/7, third=third_a7, w3=0.3)`(최종 가중 0.4/0.3/0.3).
- 변형: `_twin_rule_testpair.csv` = 위 + test 내 동일 행 쌍 규칙(8행, SARC/PRAD 4쌍 → KIPAN·KIRC). 20차는 a7 단일 요소 귀속을 위해 규칙본을, 확정되면 testpair 포함본을 권함.

## 20차 결과 — v10 LB 0.4483029809 (16차 −0.0081) 와 분해
- CV 조건 3개(+0.0108, 31~100↑, STES↓)를 모두 채우고도 하락. **CV 이득이 LB로 안 옮겨진 세 번째 사례**(11차·19차·20차). 옮겨진 것은 16차 하나.
- 행 단위 분해: 17차와 다른 308행 중 **207행이 상피암 군집(STES·LUSC·HNSC·LUAD·BLCA·COAD·CESC) 안의 재배치**(STES→LUSC 32, STES→COAD 19, COAD→STES 15, STES→LUAD 8…). 19차(−0.0136)도 같은 군집 재배치(UCEC/LUSC/LUAD→STES 41 등)였다. 반대로 17·18차(0 변화)는 이 군집을 거의 안 건드렸다.
- **가설: 상피 군집은 test 분포 이동(31~100 구간 2.4배)의 핵심이라, train CV로 얻은 어떤 재배치도 test에서는 손해.** 16차 모델의 상피 군집 결정이 test에 대해 국소 최적에 가깝다.
- **하이브리드 `approach14_v10_20260914_0110_hybrid_epifreeze.csv`**: 상피 7클래스가 관여하는 행(17차 예측 또는 v10 예측이 상피)은 17차를 유지, 나머지만 v10. 정직 CV 0.5013(+0.007; 이득은 DLBC +0.05, KIRC +0.03, BRCA +0.02, UCEC +0.02, PAAD +0.02, TGCT +0.02 / THYM −0.02). 17차와 **101행 차이**(PAAD→OV 6, KIPAN→KIRC 6, PRAD→SARC 6, BRCA→OV 5…), STES 비율 17차와 동일. `_testpair` 변형(+8행)도 생성. LB 피드백으로 군집을 골랐다는 점에서 순수 train 선택은 아님을 명시.

## 초과변이 규칙 (2026-09-14 오전) — test 분포 이동을 직접 겨냥한 첫 규칙
- **발견**: test에서 변이 888~1,017개인 행 18개가 ACC로 확신도 0.62~0.83으로 예측됨(17차). train에서 변이 396개(ACC 최대) 초과 행 61개의 라벨은 SKCM 17·UCEC 15·STES 12·COAD 9·BRCA 3·CESC 2·DLBC 1·PAAD 1로 **ACC 0**. 이 18행은 ACC의 정확 변이 표지(SOWAHC L42L 등 12개)를 0~2개만 보유(train ACC 평균 2.72, train 초과변이 행 0.0) → ACC 배치가 아니라, 초과변이가 ACC 표지 **유전자**를 다른 위치에서 건드려 유전자 단위 점수가 오작동한 것. test 초과변이 행 119개(4.7%, train 1.0%)는 문서 05의 "초과변이 5.6% vs 1.6%" 분포 이동 그 자체.
- **규칙** `postprocess/hypermut_rule.py`: n_mut > 396이고 예측 클래스가 train 초과변이 행에 2회 미만인 클래스면, 허용 집합 {BRCA, CESC, COAD, SKCM, STES, UCEC} 안에서 배율 적용 확률 argmax로 교체. 임계·집합 모두 train으로만 결정.
- **OOF에서는 검증 불가**: train 초과변이 61행은 이미 OOF 정답률 0.77로 잘 맞아 규칙이 2행만 건드림(−0.0009). 현상이 test에만 있으므로 판단 근거는 CV가 아니라 (a) train ACC 0/61, (b) 표지 부재, (c) test ACC 예측 39행 중 18행(46%)이 초과변이라는 생물학적 비현실성(MSI형 ACC는 수 %).
- **적용 결과**: 17차 → 31행 변경(ACC→STES 9·UCEC 5·CESC 3·BRCA 1, DLBC→STES 2·UCEC 2·BRCA 1, LUSC→BRCA 2·CESC 1, SARC→UCEC 2). 하이브리드 → 30행(CESC 10으로 쏠려 train 빈도와 어긋남 → 17차 기반을 권함). 기대: ACC 정밀도 21/39→21/21 수준이면 ACC F1 단독으로 +0.005~0.008, 옮겨간 행 절반만 맞아도 STES·UCEC 소폭 +. 파일 `twin_rule/approach14_v3_20260913_2132_twin_rule_hypermut.csv` (+`_testpair` 39행).
- 상피 동결 가설과의 관계: STES로 9행 유입이지만 "군집 내 재배치"가 아니라 "확실히 틀린 ACC에서의 유입"이라 성격이 다름.

## 21차 결과 — 초과변이 규칙 LB 0.468911912 (2026-09-14) — **팀 최고 갱신, 16차 +0.0125**
- 31행 변경으로 +0.0125. 행당 효과가 지금까지 어떤 제출보다 크다(16차는 수백 행 변경으로 +0.014). 메커니즘: ACC 예측 39행 중 확실히 틀린 18행을 걷어내 ACC 정밀도가 회복(ACC F1 하나가 macro의 1/26 = 0.038 비중)되고, 옮겨간 행 일부가 STES·UCEC·CESC 정답이 됨.
- **교훈**: LB를 올린 두 사례(16차, 21차)는 모두 "test에 많고 train에 적은 영역(변이 31~100, 초과변이 >396)"을 직접 다뤘다. train CV로는 보이지 않는 분포 이동을 train 통계 + 도메인 근거(ACC는 초과변이 클래스가 아님)로 교정하는 것이 이 대회의 핵심 축. **CV 이득 ≠ LB 이득**이라는 원칙의 반대편: **CV에 안 보여도 test 분포 근거가 확실하면 LB 이득**.
- 다음: (1) 21차 + test 동일 행 쌍 규칙 8행(`_hypermut_testpair.csv`, 39행) (2) 초과변이 규칙의 임계·허용 집합 감도(300/396/500, DLBC 포함 여부)를 train 통계로 점검 (3) 같은 논리로 "train에 없는 조합"을 더 찾기: 클래스별 변이 수 하한, 저변이 행의 고변이 클래스 예측 등 (4) 하이브리드(상피 동결 101행)는 초과변이 규칙 위에서 별도 제출로 판정.

## 초과변이 규칙 감도·확장 점검 (21차 직후)
- 임계 둔감: 200~396 밴드로 규칙을 확장해도(허용 집합 = 그 밴드 train ≥2회) 바뀌는 test 행 0. 저변이 쪽(변이 0~4)에는 train에 없는 클래스 예측이 없음 → 이 논리는 초과변이 쪽에서만 작동.
- 21차 기준 test 초과변이 119행 예측: STES 43·UCEC 39·COAD 22·CESC 8·BRCA 5·SKCM 2. train >396(61행)은 SKCM 28%인데 test 예측 SKCM 2%로 큰 차이 — SKCM 초과변이(UV형)를 모델이 못 잡는 것일 수 있으나 train만으로 강제할 근거 없음.
- **hypermut2 변형**(`_hypermut2.csv`, +`_testpair`): >600 밴드 허용 집합을 따로(train >600: UCEC 12·SKCM 7·COAD 5·STES 4·BRCA 2, CESC 0) → 21차의 CESC 8행을 BRCA 4·STES 2·UCEC 1·SKCM 1로. 근거가 약함(train >600 32행뿐, 대안 확률 0.04~0.12) → 우선순위 낮음, testpair·하이브리드 뒤.

## 22차 결과 — 21차 + test 동일 행 쌍 규칙 8행: LB 0.4682665 (21차 −0.0006) → 규칙 폐기
- 8행(SARC·PRAD 4쌍 → KIPAN·KIRC)이 소폭 손해. 해석: 쌍 배정이 동전 던지기라 기대값 0 근처였고 실현값이 음수. 또는 test에서 변이 ≥3 동일 행 쌍 중 신장·뇌가 아닌 쌍이 존재(train 422그룹 100%였지만 표본 밖). 21차가 기준선.
- 하이브리드는 21차 위에 다시 구성(`approach14_v10_20260914_0110_hybrid_on21.csv`): 비상피 행만 v10, 초과변이 규칙은 17차 확률로 재적용.

## 마커 일관성 규칙 (초과변이 규칙의 일반화) — 정직 OOF에서 실패, 폐기
- `postprocess/marker_consistency_rule.py`: train 보유자 ≥30인 hotspot 위치 토큰(19개, 예 BRAF:600 → THCA·SKCM·COAD·LUAD·GBMLGG)마다 허용 클래스(보유자 ≥2)를 두고, 비허용 예측을 허용 집합 안에서 재선택.
- 정직 OOF(fold 학습부로 fit): 76행 변경, 정답 34 → 10, **macro F1 0.4941 → 0.4877**. 모델이 마커 통계보다 문맥을 잘 읽는다. 21차 적용 시 44행(BLCA→STES 4, LUSC→STES 3 …)이었으나 제출하지 않고 파일 삭제.
- **교훈**: 21차가 통한 것은 "규칙"이라서가 아니라 (a) test에만 있는 분포(초과변이 4.7배) (b) 근거가 0/61로 절대적 (c) 대안이 train 초과변이 클래스로 자연스러움, 세 조건 때문. 조건 (a)가 없는 일반 마커 규칙은 train CV에서 이미 손해.
- BRAF V600 보유 행의 STES·LUSC 예측 7행(train 339행 중 0)은 남은 소규모 후보이나, 일반 규칙이 OOF에서 실패한 이상 별도 근거 없이는 보류.

## 기준 변경(9/14 오후, test 분석 금지)에 따른 정리
- train 전용 일반형 "클래스별 변이 수 범위 규칙"(`postprocess/class_range_rule.py`): 정직 OOF 0.4941 → 0.4915(k_max=1, 14행 정답 9→1) / 0.4906(k_max=2). **실패 → 미채택, 파일 삭제.** 21차의 +0.0125는 이 일반형이 아니라 test 예측 점검으로 ACC를 특정한 데서 나온 것임을 명시.
- 제외: 상피 동결 하이브리드(`hybrid_on21` 등), hypermut2, test 쌍 규칙. 이후 후보는 정직 CV로 검증된 train 전용 변경만.

## v11·v12 — 아미노산 치환 스펙트럼 (2026-09-14 16:27) — train 전용, 지금까지 최대 CV 이득
- 도메인 지식: 변이 서명(UV→SKCM, 흡연→LUAD·LUSC, POLE→UCEC, MSI→COAD·STES, APOBEC→BLCA·CESC·HNSC)은 특정 뉴클레오타이드 치환이라 아미노산 치환 쌍 분포로 드러난다. 기존 피처는 유전자 단위·심각도만 봤고 **치환 쌍의 분포**는 없었다.
- `features/spectrum.py`: 행 단위 missense 20×20 치환 쌍 비율(380열, 합 1), 성질 클래스 5×5 접힘(25열), 변이 유형 비율(missense/동의/종결/프레임시프트/기타, 절단 비율). **fit 통계 없음(행 내부 정규화만)** → test 누수 여지 없음. 피처 종류 `--features v4s`(v4+스펙트럼), `v4sp`(v4p+스펙트럼).
- 정직 CV(배율 적용):

| 구성 | 전체 | 0~10 | 11~30 | 31~100 | 101+ | 절반 교차 Δ |
|---|---|---|---|---|---|---|
| 9차 단독 | 0.4847 | 0.242 | 0.387 | 0.403 | 0.359 | |
| **v4s 단독** | **0.5035** | 0.240 | 0.421 | 0.464 | 0.436 | +0.016 / +0.004 |
| v4sp 단독 | 0.4980 | 0.239 | 0.403 | 0.483 | 0.378 | |
| 16차 9차+v2 | 0.4941 | 0.255 | 0.382 | 0.436 | 0.376 | 0 / 0 |
| **v11 v4s+v2** | **0.5137** | 0.255 | 0.422 | 0.464 | 0.396 | **+0.025 / +0.015** |
| v4s+v2 .6/.4 | 0.5177 | | | | | |
| **v12 4모델 1/4** | **0.5172** | 0.257 | 0.411 | 0.467 | 0.395 | +0.030 / +0.017 |

- 클래스별(v4sp − v2): CESC +0.10, PAAD +0.07, HNSC +0.07, TGCT +0.07, LUSC +0.06 / BLCA −0.04, DLBC −0.02. 상피 클래스가 서명(APOBEC·흡연)으로 갈린다 — 재배치가 아니라 새 신호.
- 주의: 19·20차에서 CV +0.007~0.011이 LB로 안 옮겨졌다. 이번은 +0.020~0.023으로 두 배 이상이고 정보원이 새것이지만 판단은 LB. 제출 순서: v11(16차에서 한 요소: 9차 → v4s) → v12(4모델).
- 기준(test 분석 금지) 준수: 스펙트럼 피처·가중치·배율·규칙 전부 train. 제출 전 점검은 형식(행 수·열·라벨 집합)만 — `run_multi`에 assert로 고정. test 예측 분포는 보지 않았다.

## 23차 결과 — v11 LB 0.4752484393 (2026-09-15) — **팀 최고, train 전용 구성으로 21차(0.4689) 초과**
- 16차 대비 +0.0188, 21차(test 점검으로 만든 규칙 포함) 대비 +0.0063. CV +0.0196이 LB +0.0188로 **거의 그대로 옮겨진 두 번째 사례**(첫 번째 16차). 공통점: 원본 표에 없던 새 정보(변이 유형 분리 / 치환 스펙트럼)를 넣은 모델의 블렌드이고, 31~100·101+ 구간이 함께 올랐다.
- 규정: 스펙트럼은 행 내부 정규화(fit 없음), 가중치·배율·규칙 전부 train. 제출 전 test 예측 분포 미확인. **누수 우려 없는 최고점.**
- 다음: v12(4모델, CV 0.5172) → v13(v4s .6 / v2 .4, CV 0.5177, v11 저장 확률 재사용) → 스펙트럼을 클래스별 집약 점수 형태로도 시험(접근 14 레시피).

## 24차 결과 — v13(v4s .6 / v2 .4) LB 0.4765141 (2026-09-15) — 팀 최고 갱신(v11 +0.0013)
- CV 0.5137 → 0.5177(+0.004)이 LB +0.0013로. 방향 일치, 크기는 편차 안. 스펙트럼 모델 쪽 가중치를 높이는 것이 손해는 아님. v12(4모델)는 미제출 상태(제출 시 기록).

## v14·v15 — 가중치 축 (2026-09-15 10:30, 저장 확률 재사용)
| 구성 | 전체 CV | 31~100 | 절반 교차 Δ vs v13 |
|---|---|---|---|
| v13 v4s .6 / v2 .4 (24차 0.4765) | 0.5177 | 0.475 | 0 / 0 |
| **v14 v4s .5 / v2 .3 / v4sp .2** | **0.5200** | 0.480 | +0.002 / +0.002 |
| v15 v4s .7 / v2 .3 | 0.5194 | 0.482 | +0.003 / +0.000 |
| v12 4모델 1/4 | 0.5172 | 0.467 | −0.002 / +0.001 |
- v12는 v13보다 낮고 31~100도 낮아 제출 가치 없음(미제출 유지). 순서: v14 → v15.

## 25차 결과 — v14(v4s .5 / v2 .3 / v4sp .2) LB 0.4784149 (2026-09-15) — 팀 최고 갱신
- CV +0.0023 → LB +0.0019. 스펙트럼 축은 세 번 연속(23·24·25차) CV 방향이 LB로 옮겨짐. 누적: 16차 0.4564 → 0.4784 (+0.022), 전부 train 전용.
- 다음: v15(v4s .7 / v2 .3, CV 0.5194) → 후속 CV(v4s colsample 0.5, spec 저차원) 결과 따라 파트너 추가.

## v4s 규제 변형 · 스펙트럼 저차원 모델 (2026-09-15 낮, `run_cv.sh`로 재실행)
| 구성 | 전체 CV | 31~100 | 절반 교차 Δ vs v14 |
|---|---|---|---|
| v14 기준 | 0.5200 | 0.480 | 0 / 0 |
| v4s colsample 0.5 단독 | **0.5142** (단일 모델 최고; v4s 0.5035) | 0.493 | |
| v14에서 v4s → col0.5 / 추가 | 0.5201 / 0.5205 | | −0.004 / +0.004 (불일치) |
| spec(스펙트럼+카운트 421열) 단독 | 0.2959 | | |
| v14 + spec .1 / .2 | 0.5225 / 0.5218 | | +0.002 / +0.003 |
| **v16 v4s .45 / v2 .25 / v4sp .15 / spec .15** | **0.5234** | 0.479 | **+0.003 / +0.004** |
- 규제 축(col 0.5)은 단독은 오르지만 블렌드에는 보탬이 없음(v4s와 정보 중복). 저차원 spec 모델은 약해도 정보가 달라 파트너로 유효 → v16 후보. spec 학습은 421열이라 1분.

## 9/15 병합 — 혜림 접근16이 v16 위에서 팀 최고(0.4799)
- 혜림 님이 v16의 네 번째 파트(spec XGB, 단독 0.30)를 **아미노산 치환 프로필 MultinomialNB**(383개 횟수: 방향 있는 치환 380 + 절단·동의·기타)로 교체 → 저장 OOF 0.5234 → 0.5306(+0.0071), 새 분할 재학습 +0.0055, LB 0.4784 → 0.4799. 같은 정보를 다른 방식(증거 합산)으로 읽는 파트너가 XGB보다 낫다는 결과. 전부 train 전용.
- 접근14의 다음 기준선은 접근16 구성. 남은 축: NB 비중(.15→.2/.25), spec XGB와 NB 동시 포함, v4s 안에 NB 점수표를 피처로 넣기(접근 14 레시피: 클래스별 집약 점수).

## 접근16 구성 주변 스캔 (2026-09-15 오후, 저장 OOF·NB OOF 재생성, 전부 train)
- NB 비중 .15 → .2/.25/.3: 0.5283/0.5189/0.5108(하락). spec XGB 동시 포함(.1): 0.5289. v4s col0.5 교체/추가: 0.5254/0.5273. v2 제외: 0.5146, v4sp 제외: 0.5294. 가중치 미세 조정 최고 0.5317(v4s.4/v2.25/v4sp.2/NB.15, 절반 불일치).
- NB 파트너 변형(블렌드 .15): MNB α0.1 0.5307, α2 0.5288, α5 0.5202, ComplementNB 0.5216, LR(횟수) 0.5229 — **α0.5 MNB(혜림 원안) 최적**.
- **결론: 접근16 v1 구성(v4s .45 / v2 .25 / v4sp .15 / NB α0.5 .15 = 0.5309)은 블렌드·파트너 축에서 국소 최적.** 남은 축은 "NB 점수표를 v4s 모델 안의 피처(클래스별 집약 점수 26열)로 넣기"(접근 14 레시피) → `v4sn` CV로.

## v4sn(NB 점수표를 피처로) · v17 · v18 (2026-09-15 14:00)
| 구성 | 전체 OOF | 31~100 | 절반 교차 Δ vs 접근16 |
|---|---|---|---|
| 접근16 v1 (0.4799) | 0.5309 | 0.505 | 0 / 0 |
| v4sn 단독 | 0.4666 | 0.451 | (v4s 0.5035보다 −0.037: NB 점수 열이 트리를 흔듦 — 피처 추가 실패 패턴) |
| v4sn / v2 / v4sp / NB | 0.5195 | | −0.026 / +0.003 |
| v17 v4s.4 / v2.25 / v4sp.2 / NB.15 | 0.5317 | 0.506 | +0.002 / −0.000 |
| **v18 v4s.25 / v4sn.2 / v2.25 / v4sp.15 / NB.15** | **0.5348** | 0.507 | **+0.002 / +0.006** |
- v4sn은 v4s를 대체하면 손해, **v4s 옆에 파트너로 두면** +0.004. 접근 14 레시피(점수 형태 피처)가 스펙트럼 NB에는 통하지 않았고, 파트너 블렌드가 답. v18 = 두 방법을 가장 깊게 합친 형태, 오늘 마지막 제출 후보(v17 대신).

## 27차 결과 — v18(5모델, v4sn 포함) LB 0.4639488 (접근16 0.4799 −0.0160, v14 −0.0145) → **v4sn 축 폐기**
- OOF +0.0039(절반 양방향 +)가 LB −0.016으로 크게 반전. 바뀐 요소는 v4sn(.2)뿐이므로 원인은 v4sn.
- **원인(가설, train 근거로 설명 가능)**: v4sn의 NB 점수표 피처는 학습 행에서는 내부 OOF(5-fold, 80% 데이터로 fit한 NB의 로그확률)이고, 추론 행에서는 전체 train으로 fit한 NB의 로그확률이다. NB 로그확률은 값 범위가 극단적(−20~0)이라 fit 데이터가 늘면 분포가 더 날카로워져 **학습 때 본 피처 분포와 추론 때 피처 분포가 어긋난다**(스태킹 피처의 고전적 함정). 접근 1·14의 cw_ 점수(토큰 수로 평균한 로그우도비, 반올림)는 이 어긋남이 작아 괜찮았지만 NB 확률은 컸다. 단독 0.4666으로 이미 v4s보다 −0.037이었던 것도 경고였다.
- **교훈 추가**: (1) 단독 성능이 골격 모델보다 크게 낮은 파트너는 OOF 블렌드 이득이 있어도 넣지 않는다(19차 LR, 27차 v4sn). (2) 스태킹형 피처(모델 확률을 피처로)는 학습/추론 분포 어긋남 위험 → 쓰지 않거나 정규화된 점수(로그우도비 평균)만.
- 기준선은 접근16 v1(0.4799). 남은 후보: v15(v4s .7/v2 .3, 단독 축), v16(spec XGB), 혜림 접근16 v2 게이트.

## v4s2 — 스펙트럼 v2(종결·프레임시프트 잔기 분포 42열 추가) — 정직 CV 실패, 폐기 (2026-09-15 저녁)
- v4s2 단독 0.4501 (v4s 0.5035 −0.053). DLBC −0.33, ACC −0.24, TGCT −0.23, KIPAN −0.13 / THYM +0.17. 블렌드도 전부 접근16 이하(v4s 교체 0.5109, 파트너 추가 0.5291).
- 원인: 행당 종결·프레임시프트 수가 0~3개라 잔기 비율이 극도로 희소·잡음 → 소수 클래스 분기가 흔들림(v1의 희소 토큰 실패와 같은 유형). missense 380쌍은 행당 수십 개라 비율이 안정적이었던 것과 대비.
- 시사점: 스펙트럼 비율은 **표본 수가 충분할 때만** 신호. 저변이 행의 missense 비율도 같은 잡음을 가질 수 있음 → 표본 수 기반 수축(shrinkage) 변형(v4s3)으로 검증.

## v4s3 — 스펙트럼 v3(표본 수 수축, K=10) — 정직 CV 실패, 폐기 (2026-09-15 저녁)
- v4s3 단독 0.4444 (v4s −0.059). 전 구간 하락(0~10 0.217, 11~30 0.342, 101+ 0.345). 블렌드도 접근16 이하(교체 0.5022, 파트너 추가 0.5309 동일).
- 해석: 저변이 행의 "희소한 원시 비율"(예: 변이 2개면 두 칸이 0.5)은 잡음이 아니라 **"어떤 치환이 있었나"의 지표 역할**을 하고 있었고, 균등분포로 당기면 그 지표가 사라진다. 스펙트럼은 **원시 비율(v4s) 그대로가 최선**. v2(잔기 분포 추가)·v3(수축) 모두 폐기 → 스펙트럼 피처 축은 v4s로 확정.
- 남은 train 전용 후보: v15(v4s .7/v2 .3, 0.5194), v16(+spec XGB .15, 0.5234), 혜림 접근16 v2 게이트. 기준선 접근16 v1(0.5309, LB 0.4799).

### 27차 하락 원인 정정 (train 근거로 재진단, 2026-09-15 밤)
- 앞서 적은 "OOF vs 전체 fit 분포 어긋남"은 작았다(snb_max·margin 평균·표준편차 거의 동일). 다만 NB 1위 클래스가 OOF와 전체 fit에서 **18.2% 행에서 다르다** → NB 자체가 불안정.
- **주원인: NB 점수표 피처가 변이 수에 따라 포화한다.** train 기준 snb_margin 평균: 변이 0~10 → 0.68, 11~30 → 0.90, 31~100 → 2.97, 101~396 → 11.9, >396 → 19.0(최대 20.7 = 로그 바닥 −20.7 대비). 즉 변이 100개 이상이면 NB는 한 클래스에 확률 ≈1을 몰아주고, 트리는 그 열을 "확정 신호"로 학습한다. train에서 그런 행은 6%(387행)뿐이라 트리가 포화 구간을 거의 못 봤고, 9/9에 기록된 대로 test는 초과변이 행이 훨씬 많다(3. docs/05, 기록용). NB가 초과변이 행에서 자주 틀린다는 것은 21차(ACC) 에피소드에서 이미 드러난 성질이라, v4sn은 **test에 많은 구간에서 NB의 과신을 증폭**한 모델이 됐다.
- 왜 정직 CV는 못 잡았나: train에 포화 구간 행이 적어 macro F1에 거의 안 실림. 파트너 블렌드(접근16)의 NB .15는 로그 평균 안에서 다른 세 모델이 상쇄하지만, v4sn은 트리가 NB 값을 분기 조건으로 써서 상쇄가 안 됨.
- 교훈(보강): 변이 수에 따라 크기가 폭발하는 피처(NB 로그확률, 누적 로그우도)는 트리 피처로 쓰지 않는다. 쓰려면 토큰 수로 나눈 평균(접근 1 cw_ 방식)처럼 크기를 정규화한다.

## v4sm — NB 점수표를 토큰 수로 정규화(유계)해 피처로 (2026-09-15 15:05) — 정직 CV 실패, 축 종료
| 모델 | 단독 CV | 0~10 | 11~30 | 31~100 | 101+ |
|---|---|---|---|---|---|
| v4s (기준) | 0.5035 | 0.240 | 0.421 | 0.464 | 0.436 |
| v4sn (로그확률 그대로, 폐기) | 0.4666 | 0.229 | 0.357 | 0.451 | 0.409 |
| **v4sm (정규화)** | **0.4475** | 0.217 | 0.363 | 0.422 | 0.323 |
- 정규화로 값의 포화는 없앴지만(margin 최대 2.6, 변이 수와 무관) 단독 성능은 오히려 더 떨어졌고, 블렌드도 전부 접근16(0.5309) 이하(교체 0.5071, 파트너 추가 0.5220).
- 해석: NB 점수표 26열은 라벨과 상관이 높지만 잡음이 큰 열이라, 트리가 이 열들로 먼저 분기하면서 스펙트럼 비율·유전자 열의 좋은 분기를 밀어낸다(v1의 "희소 토큰이 저변이 클래스를 망침"과 같은 유형). 포화(27차 LB 하락 원인)와 별개로 **"NB 점수를 트리 피처로" 축 자체가 성립하지 않음**. NB는 파트너(접근16 v1)로만 쓴다.
- 스펙트럼 관련 파생 축 결산: v2(잔기 분포)·v3(수축)·v4sn·v4sm 모두 폐기. 유효한 것은 v4s 원시 비율 + NB 파트너 .15 뿐.
- (같은 날) 스펙트럼 LR을 NB 옆 추가 파트너로(.1): 비율 C1/C10, 횟수 C0.05 전부 0.5278~0.5289 < 0.5309, NB 대체도 ≤0.5233. **파트너 축도 NB 하나로 충분.**

## 28차 결과 — 혜림 접근16 v2 게이트 LB 0.4687 (접근16 v1 0.4799 −0.0112)
- 저장 OOF 재계산 0.5320(+0.0011, 절반 +0.0006/+0.0018)이 LB −0.011로 반전. 게이트는 train에서 21%(1,296행), test에서 739행(29%, 혜림 문서)의 답을 바꾸는 큰 변경인데 CV 이득은 +0.001 — **바뀐 행 수 대비 CV 이득이 너무 작은 변경은 LB에서 어느 쪽으로든 튄다**(19·20차와 같은 유형).
- 지금까지 LB로 옮겨진 변경: 새 정보 모델 추가(16·23차, CV +0.01~0.02) 및 소폭 가중치 조정(24·25차, 바뀐 행 적음). 옮겨지지 않은 변경: 같은 정보의 재표현(19·20·27차), 수백 행을 바꾸는 규칙형 조정(28차).
- 남은 후보 v17(가중치만 조정, CV +0.0008)은 24·25차형이라 위험은 낮으나 기대도 작음.

## ⚠️ 버그 발견·정정 (2026-09-16 10:30) — v4sn·v4sm·v4s2·v4s3·v4st CV는 무효였음
- `main.py` FeatureMaker에 새 피처 종류를 등록할 때 첫 번째 목록(기본 분기)에만 추가하고 **접근 1 점수(cw_ 140열)·인사이트·지식 피처 분기 목록(fit 3곳, transform 3곳)에는 빠뜨렸다.** 그래서 이 다섯 종류의 모델은 v4 골격 없이(cw_ 0열) 학습됐고, 단독 CV가 일제히 0.44~0.47로 나온 것은 피처가 나빠서가 아니라 골격이 빠졌기 때문. v4s·v4sp는 처음에 여섯 목록 전부에 등록돼 정상(열 4,255 = v4 3,844 + 스펙트럼 411).
- 확인: 수정 후 400행 기준 열 수 v4s 4,255 / v4st 4,284 / v4s3 4,256 / v4sn 4,283, cw_ 140·인사이트/지식 118 모두 포함.
- **무효 처리되는 결론**: "v4s2 잔기 분포 실패", "v4s3 수축 실패", "v4st TP53 실패", "v4sn·v4sm NB 피처 실패"(단독 수치 전부). 27차(v18) 하락도 v4sn이 골격 없는 약한 모델이었던 것이 주원인일 수 있음(포화 가설은 부차). 다섯 종류 정직 CV를 `run_cv.sh`로 순차 재실행 중(약 50분).
- 재발 방지: 피처 종류 등록은 한 곳에서 하도록 리팩터 예정(지금은 7개 목록 수동). 새 종류 등록 뒤 반드시 열 수·cw_ 열 수를 v4s와 비교하는 점검을 `run_cv.sh` 앞단에 추가.

## 버그 수정 후 재실행 결과 (2026-09-16 11:05) — 이전 "실패" 결론 정정
| 모델 | 단독 CV (정정) | 이전(무효) | 접근16 블렌드에 파트너로(.2) | 절반 Δ | train 바뀐 행 |
|---|---|---|---|---|---|
| v4s (기준 골격) | 0.5035 | | 0.5309 (접근16 v1) | | |
| **v4sn** (NB 로그확률 피처) | **0.5324** (단일 모델 최고) | 0.4666 | **0.5352 (+0.0043)** | **+0.003 / +0.006** | 359 |
| v4sm (NB 정규화 피처) | 0.5177 | 0.4475 | 0.5257 | −0.010 / +0.001 | 463 |
| v4s2 (잔기 분포) | 0.5112 | 0.4501 | 0.5308 | −0.001 / +0.001 | 198 |
| v4st (TP53 스펙트럼) | 0.5047 | 0.4476 | 0.5309 | −0.001 / +0.001 | 175 |
| v4s3 (수축) | 0.5006 | 0.4444 | 0.5308 | −0.001 / +0.001 | 216 |
- **정정된 결론**: NB 점수표를 피처로 넣는 것은 실패가 아니라 **단일 모델 최고(0.5324)**. v4s2·v4st·v4s3는 단독은 v4s와 비슷하거나 약간 위이지만 블렌드에 보탬이 없음(정보가 v4s와 겹침). v4sm(정규화)은 v4sn보다 낮음 → 포화 제거가 오히려 손해.
- 27차(v18) 하락의 주원인은 골격 없는 v4sn(0.4666)이었음. 포화 가설은 부차로 격하. 단, 수정된 v4sn도 NB 로그확률 피처를 그대로 쓰므로 초과변이 구간 위험은 남아 있음(train 101+ 구간 0.399로 v4s 0.436보다 낮은 점이 그 흔적).
- **v19** = v18과 같은 가중(v4s .25 / v4sn .2 / v2 .25 / v4sp .15 / NB .15), 수정된 v4sn. 정직 CV 0.5352, train 바뀐 행 359(v14형 규모), 절반 양방향 +. 오늘 후보.

## 29차 — v17 LB 0.4785452 (접근16 v1 −0.0014). 소폭 가중치 조정도 이제 정체. CV 0.52 이상에서 LB 천장 ≈0.48 재확인. 다음 v19(천장 시험).

## 30차 결과 — v19 LB 0.4802967 (2026-09-16) — **팀 최고 갱신(접근16 v1 +0.0004)**
- 버그 수정된 v4sn(NB 점수표 피처, 단독 0.5324)을 파트너 .2로 넣은 5모델. CV +0.0043 → LB +0.0004. 스태킹형이지만 골격을 갖추자 무너지지 않았고, 천장(0.48)을 조금 넘음. 27차 하락은 골격 누락 때문이었음이 LB로도 확인.
- v4sn 비중 스캔(저장 OOF): .2(v19) 0.5352 / .25 0.5367 / .3 0.5360~0.5374 / .4 0.5381(절반 +0.0009/+0.0055, 346행) / .35(v4s .1·v2 .2) 0.5212(↓). **v20** = v4sn .4 / v4s .1 / v2 .2 / v4sp .15 / NB .15, 저장 확률 재사용, md5 재현. 오늘 마지막 제출 후보.
- 내일 축: v4sn 자체 보강(NB 피처 α, 내부 OOF fold 수), v4sn을 v4sp 골격 위에도(v4spn), 가중치 미세 조정.

## 31차 — v20(v4sn .4) LB 0.4810081 — 팀 최고 갱신(v19 +0.0007). NB 피처 모델 비중을 올릴수록 LB도 오름(.2 → .4). 9/17 축: 비중 .5 이상, v4sn 보강(α·내부 fold), v4spn(v4sp 골격 + NB 피처).

## v20 주변 비중 스캔 (9/17 아침, 저장 OOF) → v21
| 구성 (v4s/v4sn/v2/v4sp/NB) | CV | 절반 Δ vs v20 | 바뀐 행 |
|---|---|---|---|
| v20 .1/.4/.2/.15/.15 (31차 0.4810) | 0.5381 | 0 / 0 | 0 |
| **v21 .1/.45/.2/.15/.1** | **0.5433** | **+0.0079 / +0.0019** | 164 |
| .1/.5/.15/.1/.15 | 0.5378 | +0.0015 / −0.002 | 207 |
| 0/.5/.2/.15/.15 | 0.5301 | ↓ | 267 |
| .1/.4/.2/.1/.2 (NB↑) | 0.5305 | ↓ | 262 |
- v4sn을 올리고 NB 파트너를 내리는 방향(둘 다 같은 NB 정보라 중복). v4s를 0으로 빼면 하락 → v4s는 소량이라도 필요. v21 저장 확률 재사용, md5 재현. 9/17 첫 제출.
