# 접근 8 — 피처 압축 (통합 피처 상위 500개)


## 개요
approach1+2+4 통합 피처를 전부 쓰지 않고 중요도 상위 500개로만 줄이면 노이즈가 줄어 CV·LB가 개선될 것이라는 가설로 시도. 순위는 학습 데이터(fold 내부)로 approach1+2+4 통합 피처 전체를 한 번 학습해 `feature_importances_` 기준으로 정함(`4. src/common/main.py`, `FeatureMaker(kind="a8")`).

## 버전
| 버전 | 파일 (src / submissions) | 구성 | 정직 CV Macro F1 | LB |
|---|---|---|---|---|
| v1 | approach8_v1_20260910_1029 | `--features a8`(approach1+2+4 통합 피처 압축), 후처리 없음 | **0.4809**(낙관) | **0.3972671492** (2026-09-10 11:07:17 제출) |

## 결론 — "적을수록 좋다" 가설 반박
- CV 0.4809 → 실제 LB 0.3973, 격차 **-0.084**로 approach5(approach1+2+3+4 통합, 격차 큼)와 비슷하게 나쁨. `01_CV_vs_LB_기록.md` 참고.
- approach5·approach8 둘 다 approach1+2+4 통합 피처(코드명 `v5`) 기반이라, 500개로 압축해도 그 안에서 뽑은 것이라 approach4(문헌) 피처의 영향이 완전히 사라지지 않음 — 압축이 CV-LB 격차의 근본 원인(문헌 피처의 test 일반화 저하)을 해결하지 못함.
- 채택하지 않음. 제출 기준선은 approach2 v2(approach1+2 통합 피처, 코드명 `v4` + 클래스 배율, LB 0.43) 유지.
