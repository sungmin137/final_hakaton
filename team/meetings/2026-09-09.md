# 회의 결과 (2026-09-09)

## 공통 접근
같은 SUBCLASS끼리 샘플을 묶고, 각 클래스의 변이를 정리한다. (→ `docs/03_subclass_profiles.md`, `src/common/analysis/subclass_profiles.py`)

## 접근 1 — 개수 기반 가중치 (담당: 성민, 브랜치 `sungmin`)
클래스별로 변이를 개수 순으로 정렬하고, 많이 나온 변이일수록 큰 가중치를 준다.
- 샘플의 변이들에 대해 클래스별 가중치를 합산 → 가장 큰 클래스로 예측 (직접 분류기)
- 같은 점수를 피처로 만들어 XGB 메인 파이프라인에 투입 (`--features v2`)
- 구현: `src/common/approach1_count_weight/count_weights.py`, 결과: `team/approaches/approach1.md`

## 접근 2 — 변이 특성 기반 가중치 (담당: 팀원)
클래스별 변이를 정리한 뒤, 각 변이의 생물학적 특성(driver/종결/프레임시프트/hotspot 등)을 조사해 특성별 가중치를 준다.
- 참고 자료: `docs/02_domain_knowledge.md`, `src/common/analysis/mutation_catalog.py` (변이 유형·hotspot·유전자 역할 분류)

## 접근 3 — 각 클래스 특징별 비교 (담당: 성민, 2026-09-09 추가)
암종마다 특징(driver / 변이 개수)에 맞는 전문가 모델을 두고 스태킹으로 합친다. `src/common/approach3_class_feature_compare/`, `team/approaches/approach3.md`
