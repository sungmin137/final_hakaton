# 최종 추론 1차 (2026-09-09, sungmin 브랜치)

test.csv는 이 단계에서 처음 열었다. 모델·피처·가중치는 모두 train만으로 만들어졌고, test는 예측 대상으로만 사용했다.

## 제출 파일
| 파일 | 내용 |
|---|---|
| `submissions/approach1_count_weight_v2_20260909-1442.csv` | v1 피처 + 접근1 점수(v2), XGB 공식 파라미터, 전체 train 학습 |
| `submissions/approach1_count_weight_v2_twin_rule_20260909-1442.csv` | 위 + 쌍둥이 규칙 후처리 (10행 변경) |

CV 기준 예상: Macro F1 약 0.49 / Acc 약 0.53 (5-Fold OOF). 리더보드 점수는 제출 후 `docs/experiments_log.md`에 기록.

## test 단계에서 관찰한 것 (추론 시점의 피처 관찰, 라벨 없음)
1. **결측 셀 237개(128행, 25열)** — train에는 없음. WT로 간주 (`load_test`에서 fillna). CNOT2 77개, TNFAIP6 65개에 집중.
2. **train과 완전 동일한 행 235개 (9.2%)**. 쌍둥이 규칙이 적용 가능한 행이지만, v2 XGB가 이미 같은 패턴을 학습해 실제로 바뀐 예측은 10행뿐.
3. **⚠️ 분포 차이**: test 변이수 중앙값 28 (train 14), 변이 300개 초과 142행 = 5.6% (train 1.6%). test에 초과변이 샘플이 3배 이상 많다.
   → 모델이 **STES를 21.5%로 과잉 예측** (train 비율 6.1%). 초과변이 샘플을 STES/SKCM/UCEC 중 STES로 몰아넣는 것으로 보임.
   → 이 차이를 test로 보정하는 것은 규칙 위반이므로 하지 않았다. 대신 train 안에서 초과변이 샘플의 분류를 개선하는 것이 다음 과제.
4. 예측 확신도 중앙값 0.49, 절반은 0.5 미만 → 확신 낮은 구간이 넓다. 앙상블/캘리브레이션 여지.

## 다음 실험 후보
- 초과변이(>300) 샘플 전용 분석: train 101명 안에서 STES/SKCM/UCEC/COAD를 가르는 신호 찾기.
- 변이 수를 log/순위로 바꿔 TMB 의존도 완화, 유형 비율 피처 강화.
- XGB 하이퍼파라미터(트리 수 100은 적음), LightGBM/CatBoost 앙상블.
